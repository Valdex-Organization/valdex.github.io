from replit_keep_alive import WaitressStart
import time

# This starts the web server so Replit stays awake
WaitressStart("Server is running!")

# Your main logic here
while True:
    print("Bot or Script is running...")
    time.sleep(60)
